<!DOCTYPE html>
<html>
<head>
  <title>{{ app_name }}</title>
</head>
<body>

  <h1>{{ app_name }}</h1>

  {% if enable_message %}
  <p>This page is rendered using <strong>Jinja2 Templates</strong> in Ansible.</p>
  {% else %}
  <p>Message is disabled.</p>
  {% endif %}

  <h2>Team Members</h2>
  <ul>
    {% for user in users %}
      <li>{{ user.name }} - {{ user.role }}</li>
    {% endfor %}
  </ul>

</body>
</html>
