# Common Role

## Purpose
The common role prepares the system with
basic packages and updates required
before application roles are executed.

## Why it runs first
Most application roles assume the system
is already updated and prepared.
