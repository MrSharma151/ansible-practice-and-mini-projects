# Webserver Role

## Purpose
This role installs and configures nginx.

## Task Breakdown
- install.yaml → installs nginx
- config.yaml → deploys website content

## Dependency
This role assumes the system is already
prepared by the `common` role.
